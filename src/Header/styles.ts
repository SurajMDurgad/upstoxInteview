import { StyleSheet } from 'react-native';

const Styles = StyleSheet.create({
  container: {
    flex: 0.1,
    backgroundColor: '#7D0F7D',
    justifyContent: 'center',
  },
  text: {
    color: 'white',
    fontWeight: 'medium',
    marginLeft: 10,
    fontSize: 20,
  }
});

export default Styles;